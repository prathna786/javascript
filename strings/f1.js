let str ="JavaScript";//variable.length (print the length of string)

console.log(str[0]);//print individual character

//template literals
let specialString = `This is a Template literals`;
console.log(specialString); 

let obj = {
    item: "Pen",
    price : 10,
};

let output = `The cost of ${obj.item} is ${obj.price} ₹`;
console.log(output);